<idea-plugin version="2">
    <id>com.thomas.checkMate</id>
    <name>CheckMate</name>
    <version>1.0</version>
    <vendor email="checkmateplugin@hotmail.com">Thomas Goris</vendor>

    <description><![CDATA[
<b>Discover unchecked exceptions that might be thrown in the statements you selected.</b><br>

Option to create/change a try catch statement for the selected exceptions.<br>
Option to include @throws clauses from JavaDocs in search results.<br>
Option to include Errors in search results.<br>
Option to search for exceptions through all overrides of an encountered method.<br>
Option to whitelist parts of the java*, org.xml*, org.omg* sources to search through (they are ignored by default).<br>
Option to blacklist certain exceptions from search results (i.e. NullPointerException).<br>
    ]]></description>

    <change-notes><![CDATA[
      Initial release.
    ]]>
    </change-notes>

    <!-- please see https://confluence.jetbrains.com/display/IDEADEV/Build+Number+Ranges for description -->
    <idea-version since-build="131"/>

    <!-- please see https://confluence.jetbrains.com/display/IDEADEV/Plugin+Compatibility+with+IntelliJ+Platform+Products
         on how to target different products -->
    <!-- uncomment to enable plugin in all products
    <depends>com.intellij.modules.lang</depends>
    -->
    <depends>com.intellij.modules.java</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Add your extensions here -->
    </extensions>

    <application-components>
        <!-- Add your application components here -->
        <component>
            <implementation-class>com.thomas.checkMate.configuration.CheckMateSettings</implementation-class>
        </component>
        <component>
            <implementation-class>com.thomas.checkMate.configuration.CheckMateConfiguration</implementation-class>
        </component>
    </application-components>

    <project-components>
        <!-- Add your project components here -->
    </project-components>

    <actions>
        <!-- Add your actions here -->
        <action id="checkMate" class="com.thomas.checkMate.FindAction" text="Find Unchecked Exceptions"
                description="Used to discover uncaught unchecked exceptions and add/update a try catch statement based on the selected exceptions">
            <keyboard-shortcut keymap="$default" first-keystroke="alt U"/>
        </action>
    </actions>
</idea-plugin>